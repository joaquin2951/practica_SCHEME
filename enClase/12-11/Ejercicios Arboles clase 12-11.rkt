
(define arbol1 '(1 (2 () ()) (3 ()())))
(define arbol2 '(a (b (c () ()) (d ()())) (e ()())))
(define arbol3 '(a (b (c () ()) (d ()())) (e (f ()())(g ()()))))

;Definir las funciones:
;(raiz A) retorna el elemento que está en la raíz del árbol A

;(hijo-izq A) retorna el subárbol izquierdo del árbol A

;(hijo-der A) retorna el subárbol derecho del árbol A


;Definir las funciones para recorrer un árbol en preorden,
;inorden y posorden. Cada función debe retornar una lista
;con los nodos que se visitan (según el tipo de recorrido). 

;Dados un árbol y un elemento cualquiera, determinar si el
;elemento pertenece al árbol.

